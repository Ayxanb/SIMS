<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane prefHeight="700.0" prefWidth="1200.0" stylesheets="@teacher.css"
      xmlns="http://javafx.com/javafx/25"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.khazar.sims.ui.teacher.TeacherController">

  <left>
    <VBox alignment="TOP_LEFT" spacing="5" styleClass="sidebar">

      <children>

        <VBox alignment="TOP_LEFT">
          <padding><Insets left="20" right="20"/></padding>
        </VBox>

        <!-- Courses -->
        <Button fx:id="btnCourses" maxWidth="Infinity"
            onAction="#handleCoursesClicked"
            styleClass="nav-btn" text="📚 Courses"/>

        <!-- Students -->
        <Button fx:id="btnStudents" maxWidth="Infinity"
            onAction="#handleStudentsClicked"
            styleClass="nav-btn" text="🧑‍🎓 Students"/>

        <!-- Attendance -->
        <Button fx:id="btnAttendance" maxWidth="Infinity"
            onAction="#handleAttendanceClicked"
            styleClass="nav-btn" text="🙋 Attendance"/>
            
        <!-- Schedule -->
        <Button fx:id="btnSchedule" maxWidth="Infinity"
            onAction="#handleScheduleClicked"
            styleClass="nav-btn" text="🗓 Schedule"/>

        <!-- NEW: Grades/Assessment -->
        <Button fx:id="btnGrades" maxWidth="Infinity"
            onAction="#handleGradesAssessmentsClicked"
            styleClass="nav-btn" text="💯 Grades/Assessment"/>

        <VBox VBox.vgrow="ALWAYS"/>

        <!-- Logout -->
        <VBox>
          <children>
            <Button maxWidth="Infinity"
                styleClass="logout-btn"
                text="↪️ Logout"
                onAction="#handleLogout"/>
          </children>

          <VBox.margin>
            <Insets bottom="20" left="20" right="20" top="10"/>
          </VBox.margin>
        </VBox>

      </children>
    </VBox>
  </left>

  <center>
    <StackPane fx:id="contentArea"
           styleClass="content-area"
           BorderPane.alignment="CENTER"/>
  </center>

</BorderPane>